// Central API configuration
// Change this single value to point to a different backend (e.g. production URL)
const API_BASE_URL = process.env.REACT_APP_API_URL || 'http://185.211.6.101:5000';

export default API_BASE_URL;
